import { ColorScheme } from '@mantine/core';

type ColorSchemeType = {
  DARK: ColorScheme;
  LIGHT: ColorScheme;
};

export const COLOR_SCHEME: ColorSchemeType = {
  DARK: 'dark',
  LIGHT: 'light',
};
